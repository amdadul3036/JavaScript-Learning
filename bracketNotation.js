var name= "Dhrubo";
console.log(name[0]); //It will give the first alphabet of the string in the variable name from the beginning.
console.log(name[name.length-6]);//It will give the first alphabet of the string in the variable name from the end.
//So if we want it from the beginning counting should start from 0; and if we want it from the end counting should start from 1.
console.log(name[4]);
console.log(name[name.length-2]);
